version: '3'

services:
  master:
    build: ./master
    volumes:
      - master-db:/app
    restart: always
  worker:
    build: ./worker
    restart: always
    deploy:
      mode: replicated
      restart_policy:
        condition: on-failure
      resources:
        limits:
          memory: 300m
    depends_on:
      - master


volumes:
  master-db:

